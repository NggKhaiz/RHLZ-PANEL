# Qovery — RHLZ Panel
application:
  name: rhlz-panel
  build_mode: DOCKER
  dockerfile_path: Dockerfile
  environment_variables:
    NODE_ENV: production
  ports:
    - port: 6767
      protocol: HTTP
